{
  "name": "fund-flow-glitch",
  "version": "1.0.0",
  "description": "Root package.json for Glitch deployment",
  "scripts": {
    "start": "cd backend && npm install && node server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
